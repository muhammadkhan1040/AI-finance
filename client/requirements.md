## Packages
framer-motion | Complex animations for steps, floating cards, and background elements
clsx | Utility for conditional classes (usually in base stack, adding just in case)
tailwind-merge | Utility for merging tailwind classes (usually in base stack)

## Notes
The application uses a specific "Galaxy" theme with complex CSS animations.
Glassmorphism effects require backdrop-filter support.
Form submission returns calculated rates directly.
